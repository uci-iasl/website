{{- $name := partial "bib/getcanonicalname" .name -}}
{{- $parts := slice -}}
{{- range $.params.nameparts -}}
  {{- with index $name .key -}}
    {{- $parts = $parts | append . -}}
  {{- end -}}
{{- end -}}
{{- return (delimit $parts " " | string) -}}
